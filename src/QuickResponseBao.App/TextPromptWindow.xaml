<ui:FluentWindow x:Class="QuickResponseBao.App.TextPromptWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="{DynamicResource Categories}" Width="460" Height="230" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" WindowBackdropType="None" ExtendsContentIntoTitleBar="False" Background="{DynamicResource QrbBackgroundBrush}">
  <Grid Margin="24"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <TextBlock Text="{DynamicResource CategoryDetails}" Style="{DynamicResource QrbSectionTitleTextStyle}"/>
    <TextBlock x:Name="PromptText" Grid.Row="1" Style="{DynamicResource QrbBodyTextStyle}" Margin="0,12,0,7"/>
    <TextBox x:Name="ValueText" Grid.Row="2" MaxLength="100"/>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="{DynamicResource Cancel}" Style="{DynamicResource QrbGhostButtonStyle}" IsCancel="True"/><Button Content="{DynamicResource Save}" Style="{DynamicResource QrbPrimaryButtonStyle}" Click="Save_Click" IsDefault="True"/></StackPanel>
  </Grid>
</ui:FluentWindow>
