<Window x:Class="QuickResponseBao.App.CategoryManagerWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Categories}" Width="700" Height="540" WindowStartupLocation="CenterOwner">
  <Grid Margin="18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <StackPanel x:Name="ActionPanel" Orientation="Horizontal"><Button Content="{DynamicResource Add}" Click="Add_Click"/><Button Content="{DynamicResource Rename}" Click="Rename_Click"/><Button Content="{DynamicResource Delete}" Click="Delete_Click"/><Button Content="{DynamicResource MoveUp}" Click="Up_Click"/><Button Content="{DynamicResource MoveDown}" Click="Down_Click"/></StackPanel>
    <DataGrid x:Name="CategoriesGrid" Grid.Row="1" Margin="4,12" ItemsSource="{Binding}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"><DataGrid.Columns><DataGridTextColumn Header="{DynamicResource Name}" Binding="{Binding Name}" Width="*"/><DataGridTextColumn Header="#" Binding="{Binding SortOrder}" Width="80"/></DataGrid.Columns></DataGrid>
    <DockPanel Grid.Row="2"><TextBlock x:Name="Feedback" VerticalAlignment="Center"/><Button DockPanel.Dock="Right" Content="{DynamicResource Close}" IsCancel="True"/></DockPanel>
  </Grid>
</Window>
